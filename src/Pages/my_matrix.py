class MyMatrix():
    def __init__(self, matrix, matrix_name, upper_headers, side_headers):
        self.matrix = matrix
        self.matrix_name = matrix_name
        self.upper_headers = upper_headers
        self.side_headers = side_headers